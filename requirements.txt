# Python 3.12
# Core dialogue, director-mode and IndexTTS MCP client runtime.
-e .

# Optional in-process CosyVoice helpers:
# pip install -e ".[local-tts]"

# Optional LangChain/LangGraph and MCP adapters:
# pip install -e ".[langchain-mcp]"
